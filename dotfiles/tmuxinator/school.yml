# ~/.tmuxinator/school.yml

name: school
root: ~/school/spring2016

windows:
  - linear:
      layout: main-horizontal
      panes:
        - cd 'linearAlgebra'
  - opSystems:
      layout: main-horizontal
      panes:
        - cd 'operatingSystems'
  - DSP:
      layout: main-horizontal
      panes:
        - cd 'digitalSignalProcessing'
  - techWriting:
      layout: main-horizontal
      panes:
        - cd 'technicalWriting'
  - probability:
      layout: main-horizontal
      panes:
        - cd 'probability'
